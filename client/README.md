# Client

This folder will contain the React/Vite browser application. Keep route-level screens in `src/pages`, reusable UI in `src/components`, and feature-specific behavior in `src/features`. Do not place server secrets or database code here.
