# Client source

Frontend source code belongs under this directory. Prefer feature folders for product behavior and keep shared components dependency-light. New route screens should be composed in `pages` rather than implemented directly in `App.jsx`.
