# Server

This folder will contain the Node.js/Express API. Keep HTTP concerns in routes/controllers, application rules in services, persistence in repositories, and request validation in validators.
